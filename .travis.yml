language: python
cache: pip
sudo: false

python:
  - "3.4"
  - "3.5"
  - "3.6"

env:
  - DJANGO=1.11
  - DJANGO=2.0

install:
  - pip install pipenv
  - pip install django==$DJANGO
  - pipenv install --dev

matrix:
    fast_finish: true

#    allow_failures:
#      - env: DJANGO=master

script: make coverage

after_success:
  - codecov
